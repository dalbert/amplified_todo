type Mortgage @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  interestRate: Float
  termMonths: Int
  startBalance: Float
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  isComplete: Boolean!
}
 